linters:
  enable-all: true
  disable:
  - goconst
  - gocritic
  - gocyclo
  - gochecknoglobals
  - gosec
  - interfacer
  - lll
  - scopelint
  fast: false

run:
  deadline: 1m

issues:
  exclude:
  - "`aliases.Schema` composite literal uses unkeyed fields"